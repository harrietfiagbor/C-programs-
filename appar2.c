 /* application of arrays*/
 #include<stdio.h>
int main()
{
	int num[10],i,n,key,flag=0;
	printf("enter the number of elements");
	scanf("%d", &n);
	for(i=0;i<n;i++)
	{
		printf("\nenter element %d",i+1);
		scanf("%d", &num[i]);
	}
		for(i=0;i<n;i++);
			printf("\nenter element:num[%d]:%d",i, num[i]);
			scanf("%d",&key);
		for(i=0;i<n;i++);
		prin	
}
