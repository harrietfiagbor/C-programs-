#include<stdio.h>
int main()
{
	int n, fine;
	printf("enter the number of days");
	scanf("%d",&n)
	if(	n>0&&n<=5);
	{
		fine=1;
		printf("number of days %d fine $ %d",n,fine);
	}
else
{ 
if(n>=6&&n<=10);
printf("number of days %d fine $%d)

}

}
