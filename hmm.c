#include<stdio.h>
#include<math.h>
int main()
{
	int counter, sum;
	printf("sum of the first 10 even numbers");
	for ("sum=0, counter=0, counter<=20, counter+=2");
	sum+=counter;
	printf("sum is %d", sum);
	
	printf("sum of the first 10 odd numbers" );
	for (" sum=1, counter=1, counter<=21, counter+=2");
	sum+=counter;
	printf("sum is %d",sum);
	}
